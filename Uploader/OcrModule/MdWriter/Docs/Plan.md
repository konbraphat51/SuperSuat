1. レイアウト・バウンディングボックスモデルにて、図の領域を検出

- モデルにはDocLayoutYolo, Yomitoku, PpStructureを選択可能にする
- 文章画像上に、検出された図領域のバウンディングボックスと、そのユニークIDを描画
- 検出するのは図（figure / image / chart）のみ。表と数式はLLMがMarkdownで書き下す
- Blockedの `Blocker` は変更せず、MdWriter側に図専用の `FigureDetector` を別に実装する

2. LLMに、文書画像を渡し、マークダウンへ変換するよう指示

- 1回のリクエストで1ページだけを書き下させる。全ページを1パスで並列に処理する
  - 各ページは、自分の先頭が前ページの段落の続きか、末尾が次ページへ続くかを、自分のページ画像から判断して継続マーカーで示す
  - 隣り合う2ページの判断が食い違う所だけ、両側の段落をテキストだけでLLMに渡して判定させる
  - （以前は、奇数ページを先に書き、偶数ページを前後のマークダウンと一緒に穴埋めさせる2パス方式だった。1パス目の完了を待つ分遅く、つなぎ目の判定もほぼ同じだったため1パスに改めた）
- オプションで、Yomitokuなどの従来型OCRで読んだ各ページのテキストを参照としてLLMに渡す
  - 文字は参照テキストから、構造は画像から取らせる
  - 参照テキストとの一致度（文字バイグラムのF1）が低いページは、より強いモデルで書き直す
- 脚注・傍注・コラムなどに対して特殊文法を定義し、LLMに使用させる（[MarkdownSyntax.ja.md](MarkdownSyntax.ja.md)）
  - 脚注はGFM形式（`[^n]` / `[^n]: …`）、傍注とコラムは `:::sidenote` / `:::column` ブロック
  - 目次は `:::toc` ブロックに、`- 番号 | 題名 | ページ` の項目を字下げで入れ子にして書く
  - 各ページのマークダウンの前に `<!--page:N-->` を置き（LLMではなく結合処理が置く）、ブロックがどのページにあるかを記録する
- 見出しはすべてレベル2として扱う（ページごとに独立して書くため、階層は揃えられない）

3. 結合したMarkdownを解析し、`OcrResult` に変換する

全体をつなぐ入口: [MdWriterOcr.ja.md](MdWriterOcr.ja.md)

English version: [Plan.en.md](Plan.en.md)
