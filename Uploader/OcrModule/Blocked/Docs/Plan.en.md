1. Split the page elements into blocks with a layout analysis model
2. (in parallel) Read the text of each block with an OCR model
3. (in parallel) Infer reading order and block type page by page with an LLM, then rank the headings into a hierarchy in one pass ([StructureOrganizer.md](StructureOrganizer.md))

日本語版: [Plan.md](Plan.md)
